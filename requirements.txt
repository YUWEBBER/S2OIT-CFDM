# =============================================================================
# SAR 图像彩色化微调扩散模型 —— 环境依赖
# 环境：Python 3.10 + CUDA 12.1（训练与 DINOv2 特征提取需要 GPU）
# 安装：
#     conda create -n sar2opt python=3.10 -y && conda activate sar2opt
#     pip install -r requirements.txt
# 指定 CUDA 版本：
#     pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
# =============================================================================

# 深度学习框架
torch==2.4.0
torchvision==0.19.0
xformers==0.0.27.post2                      # 高效注意力，与 torch 2.4.0 严格对应

# 扩散模型与 Hugging Face 组件
diffusers==0.25.1
transformers==4.35.2
tokenizers==0.15.2
accelerate==1.12.0
peft==0.10.0
huggingface-hub==0.25.2
safetensors==0.7.0

# 图像与数值计算
numpy==1.26.4
opencv-python==4.6.0.66
pillow==10.4.0
scipy==1.15.3
scikit-image==0.25.2
scikit-learn==1.7.2
timm==1.0.22

# 损失与评价指标
lpips==0.1.4                                # 感知损失
clean-fid==0.1.35                           # FID 特征提取
vision-aided-loss @ git+https://github.com/nupurkmr9/vision_aided_loss.git   # VAGAN 判别器
antialiased-cnns==0.3                       # vision-aided-loss 依赖
gdown==4.4.0                                # vision-aided-loss 权重下载
clip @ git+https://github.com/openai/CLIP.git   # vision-aided-loss 的 CLIP 主干
ftfy==6.3.1
regex==2025.11.3

# 日志与工具
swanlab==0.10.0                             # 实验记录（唯一日志后端）
tqdm==4.67.1
